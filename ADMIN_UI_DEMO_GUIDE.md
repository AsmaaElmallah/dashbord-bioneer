# دليل عرض لوحة تحكم بيانور للعميل

> **Prompt 29** — سكربت عملي لعرض النسخة UI على العميل أو صاحب المنتج.

---

## الهدف من لوحة التحكم

لوحة **بيانور Admin** هي واجهة تخطيطية لإدارة:

- المستخدمين وأطفالهم وبياناتهم
- الاشتراكات والباقات
- المناهج (قرآن، حساب، بصري، عاطفي)
- المكتبة والوسائط والأنشطة والتقييمات
- المجتمع والدعم والشكاوى
- **توجيه المحتوى** (لمن يظهر المحتوى، أين، ومتى)
- الملفات والأصول والإشعارات والتقارير والإعدادات

**الغرض من العرض:** الموافقة على **الشكل، الأقسام، وتدفق العمل** قبل بناء Backend حقيقي.

---

## مهم: هذه النسخة UI فقط

| ✅ موجود | ❌ غير موجود |
|----------|-------------|
| كل الشاشات والتنقل | قاعدة بيانات |
| بيانات تجريبية (mock) | API / سيرفر |
| معاينات وتفاصيل | تسجيل دخول حقيقي |
| أزرار حفظ/نشر/فحص/تصدير | رفع ملفات فعلي |
| snackbar «UI فقط» | دفع Apple/Google/Visa |
| بحث وفلاتر محلية | إشعارات push حقيقية |

**لا تغيّر هذه اللوحة أي شيء في تطبيق Flutter على الهاتف** — المشروعان منفصلان.

---

## طريقة التشغيل

```bash
cd bayanour_admin_ui
npm install          # أول مرة فقط
npm run dev
```

افتح المتصفح على:

**http://localhost:5173**

(أو المنفذ الذي يظهر في الطرفية إذا اختلف)

للعرض على شاشة كبيرة: استخدم Chrome/Edge بعرض كامل.  
للموبايل: DevTools → Toggle device toolbar، أو صغّر نافذة المتصفح — اللوحة responsive.

---

## ماذا تفتح أولاً؟

1. تأكد أن الصفحة الرئيسية هي **نظرة عامة** (`/`).
2. أشر للعميل إلى:
   - الشريط الجانبي (كل أقسام التطبيق)
   - شريط «**UI فقط**» أعلى الصفحة
   - حقل البحث العلوي (mock)

**جملة افتتاحية مقترحة:**

> «هذه لوحة تحكم مقترحة لتطبيق بيانور. كل الأرقام والبيانات للعرض فقط — الهدف أن نتفق على الشكل والأقسام قبل ربط السيرفر.»

---

## ترتيب الجولة المقترحة (15–25 دقيقة)

| # | الصفحة | المسار | مدة تقريبية |
|---|--------|--------|-------------|
| 1 | نظرة عامة | `/` | 3 د |
| 2 | المستخدمون والأطفال | `/users` | 3 د |
| 3 | الاشتراكات | `/subscriptions` | 2 د |
| 4 | المناهج | `/curriculum` | 3 د |
| 5 | قرآن (مثال مسار) | `/quran` | 2 د |
| 6 | توجيه المحتوى | `/targeting` | 4 د |
| 7 | المكتبة | `/library` | 2 د |
| 8 | المجتمع والدعم | `/community` | 3 د |
| 9 | الملفات والأصول | `/assets` | 2 د |
| 10 | الإعدادات | `/settings` | 1 د |

**صفحات اختيارية** إذا سأل العميل: التقارير `/reports`، الإشعارات `/notifications`، CMS `/cms`.

---

## ماذا تقول عند كل محطة؟

### 1. نظرة عامة (`/`)

**افتح:** البطاقات الإحصائية، تقدم المناهج، التنبيهات، الإجراءات السريعة.

**قل:**

> «من هنا يرى المدير صورة سريعة: عدد المستخدمين، تقدم المناهج، وأهم التنبيهات — مثل ملفات mp3 ناقصة أو فيديوهات onboarding غير مرفوعة بعد. هذه reflect الواقع الحالي لمشروع Flutter.»

**جرّب:** اضغط «فحص ملفات ناقصة» أو «نشر إشعار» — يظهر snackbar **UI فقط**.

---

### 2. المستخدمون والأطفال (`/users`)

**افتح:** تبويب المستخدمين → اضغط صفاً → لوحة التفاصيل.

**قل:**

> «إدارة أولياء الأمور والأطفال: الباقة، آخر نشاط، تقدم المناهج، ملاحظات السلوك. في النسخة الحقيقية تُجلب من API.»

**جرّب:** الفلاتر، chips الحالة، «محاكاة تحميل».

---

### 3. الاشتراكات (`/subscriptions`)

**قل:**

> «الباقات مطابقة لتطبيق Flutter: ذهبية، فضية، شهرية، برونزية. الدفع غير متصل — للتخطيط فقط.»

---

### 4. المناهج (`/curriculum`) وصفحات المسارات

**افتح:** بطاقات المسارات الأربعة → الخط الزمني → جدول خريطة المنهج → «فتح إدارة المسار» (مثلاً `/quran`).

**قل:**

> «مركز تحكم واحد لكل المناهج. كل مسار له صفحة إدارة: القرآن 120 جلسة، الحساب 237 شريحة، البصري 292، العاطفي 162 — الأرقام من كود Flutter.»

**في صفحة القرآن `/quran`:**

> «هنا نراجع الختمات، القرّاء، الجلسات، وحالة ملفات الصوت — حالياً manifest فقط بدون mp3.»

**في الحساب `/math` أو البصري `/visual`:**

> «جدول الدروس والشرائح والحزم — يساعد فريق المحتوى على معرفة ما هو منشور وما يحتاج مراجعة.»

---

### 5. توجيه المحتوى (`/targeting`) — **محطة مهمة**

**افتح:** مثال جاهز → معالج 4 خطوات → لوحة المعاينة على اليمين.

**قل:**

> «هذه الصفحة تجيب: **لمن** يظهر المحتوى، **أين** في التطبيق، **متى** و**بأي شروط**. مثلاً: فيديو رياضة لعمر 4–6 أشهر للمشتركين فقط.»

**جرّب:**

1. غيّر **نوع المحتوى** (فيديو / شريحة / إشعار).
2. غيّر **الجمهور** (فئة عمرية).
3. غيّر **مكان الظهور** (الرياضة، المكتبة، …).
4. فعّل/ألغِ **شروط الظهور** (دائم، يوم منهج، بعد اختبار، …).
5. اقرأ المعاينة: **«سيظهر عند…»** و **«سيظهر في…»** و **«لـ…»**.

**قل:**

> «المعاينة تشرح للمدير بالعربي أين سيرى الطفل المحتوى — بدون كتابة كود. الحفظ mock فقط حتى نربط Backend.»

---

### 6. المكتبة (`/library`)

**قل:**

> «إدارة روابط YouTube حسب الفئات: طبيعة، موسيقى هادئة، تهويدات. فحص الروابط mock — في الإنتاج نربط YouTube API أو قائمة مراجعة يدوية.»

---

### 7. المجتمع والدعم (`/community`)

**افتح:** تبويب الشكاوى → Kanban → تفاصيل تذكرة.

**قل:**

> «شكاوى واقتراحات واستشارات ونادي الأمهات — من بيانات mock مشابهة لشاشات Flutter. الرد والإغلاق UI فقط.»

---

### 8. الملفات والأصول (`/assets`)

**افتح:** تنبيهات mp3/onboarding → قسم «ملفات مرفوعة» (فارغ) → جدول manifest.

**قل:**

> «خريطة كل الأصول: صور، manifests، صوت، فيديو. يوضح ما هو موجود وما هو ناقص في repo الحالي.»

---

### 9. الإعدادات (`/settings`)

**قل:**

> «إعدادات عامة، لغات، أدوار، مصفوفة صلاحيات، حالات نشر، تكاملات مستقبلية — كلها للتخطيط. Firebase وPayment معلّمة «غير متصل».»

---

## حدود النسخة الحالية

1. **لا Auth** — أي شخص يفتح الرابط يرى اللوحة (مناسب للعرض المحلي فقط).
2. **لا persistence** — أي تعديل يضيع عند refresh.
3. **بيانات ثابتة** في `src/data/mockData.js`.
4. **فجوات حقيقية في التطبيق** تظهر في اللوحة: mp3 قرآن، فيديوهات onboarding.
5. **لا ESLint/CI** — مشروع عرض سريع.
6. **Prompts 26–27** مغطاة ضمن البناء المرحلي 01–25.

---

## ما المطلوب لاحقاً لتحويلها لمنتج حقيقي

| المرحلة | المطلوب |
|---------|---------|
| 1 | Backend (Node/Firebase/Supabase…) + API |
| 2 | Auth + أدوار (Super Admin, Content Manager, …) |
| 3 | ربط DB بمستخدمي التطبيق وأطفالهم |
| 4 | رفع وإدارة أصول (S3 / Firebase Storage) |
| 5 | CMS حقيقي + workflow نشر (مسودة → مراجعة → منشور) |
| 6 | تكامل دفع (Apple / Google / بوابة) |
| 7 | Push notifications + analytics |
| 8 | ربط YouTube / فحص روابط المكتبة |
| 9 | اختبارات + نشر (Vercel/Netlify + domain) |

---

## كيف أستخدم هذا الملف أثناء الـ Demo

1. **قبل الاجتماع:** شغّل `npm run dev` وتأكد من فتح `/`.
2. **افتح هذا الملف** على شاشة ثانية أو اطبعه.
3. **اتبع «ترتيب الجولة»** من أعلى لأسفل.
4. **استخدم جمل «ماذا تقول»** حرفياً أو بأسلوبك — هي جاهزة للعميل غير التقني.
5. **عند أي زر حفظ/نشر:** أشر إلى snackbar «UI فقط» — هذا يطمئن العميل أن لا شيء يُكسَر.
6. **اختتم بـ «حدود النسخة» + «ما المطلوب لاحقاً»** — لتحويل النقاش إلى خطة تنفيذ.

---

## مراجع داخل المشروع

- [README.md](./README.md) — تشغيل سريع
- [ADMIN_UI_AUDIT.md](./ADMIN_UI_AUDIT.md) — مراجعة Prompt 25
- [ADMIN_UI_DISCOVERY.md](./ADMIN_UI_DISCOVERY.md) — اكتشاف هوية التطبيق

**تأكيد:** لوحة التحكم **UI فقط** — لا Backend، ولم يُعدَّل مشروع Flutter `bayanour/`.

---

## خطوات تجربة الـ Clickable Prototype (Prompt 30)

1. **Overview** (`/`) — اضغط «اختصارات النموذج» أو sidebar.
2. **Users** — تبويب **الأطفال** → يُختار أول طفل تلقائياً → غيّر الصف → تظهر **لوحة التفاصيل** أسفل/بجانب الجدول.
3. **Content Targeting** (`/targeting`) — اضغط **مثال جاهز** → غيّر نوع المحتوى/الجمهور/المكان → شاهد **المعاينة** تتحدث فوراً.
4. **Community** (`/community`) — تبويب **الشكاوى** → أول تذكرة تُختار تلقائياً → اضغط Kanban/جدول → **تفاصيل التذكرة** على اليمين.
5. أي زر **حفظ/نشر/فحص** → snackbar «UI فقط».
6. **Refresh** الصفحة → كل الاختيارات تُعاد للافتراضي (لا حفظ).
